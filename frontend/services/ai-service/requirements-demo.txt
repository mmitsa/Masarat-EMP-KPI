# Lightweight requirements for Demo mode
# No torch, transformers, or heavy ML dependencies

# FastAPI & ASGI Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Pydantic for data validation (use latest compatible)
pydantic>=2.10.0
pydantic-settings>=2.1.0

# HTTP & async
httpx>=0.26.0
aiofiles>=23.2.1

# Fallback AI services (optional)
groq>=0.4.2
openai>=1.10.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.3

# Monitoring (optional)
prometheus-client>=0.19.0
